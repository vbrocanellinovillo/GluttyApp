import ProductsList from "../../components/Products/ProductsList";

export default function Products() {
  // Todos
  // 1. Ver que tarda mucho si son muchos productos
  // 2. Si selecciono una marca (o tipo) despues los filtros solo me muestran esa seleccionada
  // 3. El filtro rapido me puede dar como resultados que no hay productos
  // 4. Nombres muy largos, productos sin nombre, ...
  // 5. Hacer pantalla de error
  return <ProductsList />;
}
